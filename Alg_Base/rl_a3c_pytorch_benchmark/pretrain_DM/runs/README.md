1. `./runs` directory is used to save tensorboard logger for dynamics model training process
2. run `cd ./Alg_Base\rl_a3c_pytorch_benchmark\pretrain_DM` to change directory to pretrain dir
3. run `tensorboard --logdir ./runs/` to launch tensorboard and open `localhost:6006` to get visualization result
